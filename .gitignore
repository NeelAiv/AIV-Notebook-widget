venv/
.venv/
__pycache__/
*.pyc
.env
history.json
connections.json
*.log
.vscode/
.idea/

# Legacy ChromaDB path (unused after pgvector migration)
chroma_db/

# Docker named volume data (optional local mirror)
pgvector_data/

# SQLite runtime DB
app/db/insight.db

# LLM config contains API keys — configure via the Settings UI or OPENAI_API_KEY env var
llm_config.json

# Notebooks with potentially sensitive query history (remove this line if you want them tracked)
notebooks/

# Local scratch / prompt drafts (not part of the app)
sql_handler_prompts_to_use.sql
